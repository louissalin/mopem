add apt-get dependencies
use options gem for command line options
add option for specifying 
  - source folder (skip download & compile from source folder)
  - skip compilation step (for testing purposes)
add support for zip files
add support for xbuild and .sln files
add option for installing in GAC (default)
add option for installing in lib folder inside .mopem
  - create lib folder in .mopem/install/[mono version]/
  - add lib folder to $MONO_PATH when using that mono version
  - overwrite when installing
split targets.yml in multiple .yml files.
  - one per target
  - multiple versions of same target in same file

create target for OpenTK
create target for MonoGames
